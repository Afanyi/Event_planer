# 📅 Events Planner (MongoDB + Docker)

Minimal full-stack project using **Node.js + TypeScript (Express + Mongoose)** and **React + TypeScript (Vite)**.

## 🚀 Quick Start
```bash
docker compose up --build
